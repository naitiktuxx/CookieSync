{
  "name": "selected-site-cookie-sync-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared WebExtensions codebase for syncing encrypted selected-site cookies through Supabase.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:brave": "node scripts/build.mjs brave",
    "build:firefox": "node scripts/build.mjs firefox",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/firefox-webext-browser": "^120.0.4",
    "esbuild": "^0.23.1",
    "typescript": "^5.6.2",
    "web-ext": "^8.3.0"
  }
}
